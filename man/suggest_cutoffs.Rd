% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parameter_scout.R
\name{suggest_cutoffs}
\alias{suggest_cutoffs}
\title{Suggest coverage cutoffs from an intergenic percentile table}
\usage{
suggest_cutoffs(x, low = "Median", high = "P75")
}
\arguments{
\item{x}{The list returned by \code{parameter_scout()}, or its
\code{percentiles} dataframe.}

\item{low}{The percentile column used for \code{low_coverage_cutoff}.}

\item{high}{The percentile column used for \code{high_coverage_cutoff}.}
}
\value{
A named integer vector, \code{low_coverage_cutoff} and
  \code{high_coverage_cutoff}.
}
\description{
Applies the percentile rule to the table returned by
\code{parameter_scout()}. By default the low cutoff is the median across
files of the per-file median intergenic coverage and the high cutoff the
median across files of the per-file upper quartile; \code{low} and
\code{high} name any other columns. A fractional median is taken up to the
next integer.
}
\details{
Returns numbers to inspect and record; it does not feed them into the
pipeline.
}
\seealso{
\code{\link{parameter_scout}}
}
